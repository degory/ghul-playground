Screenshots linked from pull request comments in this repository, one directory
per pull request number.

This branch is deliberately unrelated to `main` and is never merged into it:
`gh` cannot attach an image to a pull request, and a picture of the site does
not belong in the history of the code that serves it.

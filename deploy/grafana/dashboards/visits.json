{
  "uid": "ghul-visits",
  "title": "Visits",
  "description": "What each visitor did, in order. Read from a copy of the analytics database that is rebuilt every few minutes; the copy is also where visits that are the site's own have already been removed, so nothing here needs to filter them.",
  "schemaVersion": 39,
  "version": 1,
  "editable": false,
  "time": {
    "from": "now-7d",
    "to": "now"
  },
  "refresh": "5m",
  "templating": {
    "list": [
      {
        "name": "days",
        "label": "Days",
        "type": "custom",
        "query": "1,7,30,90",
        "current": {
          "text": "7",
          "value": "7"
        },
        "options": [
          {
            "text": "1",
            "value": "1",
            "selected": false
          },
          {
            "text": "7",
            "value": "7",
            "selected": true
          },
          {
            "text": "30",
            "value": "30",
            "selected": false
          },
          {
            "text": "90",
            "value": "90",
            "selected": false
          }
        ]
      }
    ]
  },
  "panels": [
    {
      "type": "table",
      "title": "Visits, most recent first",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "analytics"
      },
      "gridPos": {
        "h": 14,
        "w": 24,
        "x": 0,
        "y": 0
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm",
        "footer": {
          "show": false,
          "reducer": [
            "sum"
          ],
          "fields": ""
        }
      },
      "fieldConfig": {
        "defaults": {
          "custom": {
            "align": "auto",
            "filterable": true
          }
        },
        "overrides": []
      },
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "rawQueryText": "select\n    substr(lower(hex(h.session)), 1, 8) as visit,\n    min(h.created_at) as started,\n    round((julianday(max(h.created_at)) - julianday(min(h.created_at))) * 86400) as seconds,\n    count(*) as hits,\n    sum(p.event = 0) as pages,\n    max(h.location) as place,\n    case when coalesce(max(h.width), 0) = 0 then 'unknown'\n         when max(h.width) < 600 then 'phone'\n         when max(h.width) < 1100 then 'tablet'\n         else 'desktop' end as screen,\n    group_concat(coalesce(nullif(replace(replace(replace(p.path, 'playground.ghul.dev/run/', 'playground-run/'), '/playground.ghul.dev', '/playground'), '/ghul.dev', ''), ''), '/'), '  ->  ') as went\nfrom hits h join paths p using (path_id)\nwhere h.created_at >= datetime('now', '-' || ${days} || ' days')\ngroup by h.session\norder by started desc\nlimit 200",
          "queryText": "select\n    substr(lower(hex(h.session)), 1, 8) as visit,\n    min(h.created_at) as started,\n    round((julianday(max(h.created_at)) - julianday(min(h.created_at))) * 86400) as seconds,\n    count(*) as hits,\n    sum(p.event = 0) as pages,\n    max(h.location) as place,\n    case when coalesce(max(h.width), 0) = 0 then 'unknown'\n         when max(h.width) < 600 then 'phone'\n         when max(h.width) < 1100 then 'tablet'\n         else 'desktop' end as screen,\n    group_concat(coalesce(nullif(replace(replace(replace(p.path, 'playground.ghul.dev/run/', 'playground-run/'), '/playground.ghul.dev', '/playground'), '/ghul.dev', ''), ''), '/'), '  ->  ') as went\nfrom hits h join paths p using (path_id)\nwhere h.created_at >= datetime('now', '-' || ${days} || ' days')\ngroup by h.session\norder by started desc\nlimit 200",
          "timeColumns": [],
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": "analytics"
          }
        }
      ],
      "description": "One row per visit, in the order the visitor went. The visit id is GoatCounter's own: random, and it lives for one visit. No address is stored anywhere, by GoatCounter or by this."
    },
    {
      "type": "table",
      "title": "Where visits started",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "analytics"
      },
      "gridPos": {
        "h": 10,
        "w": 12,
        "x": 0,
        "y": 14
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm",
        "footer": {
          "show": false,
          "reducer": [
            "sum"
          ],
          "fields": ""
        }
      },
      "fieldConfig": {
        "defaults": {
          "custom": {
            "align": "auto",
            "filterable": true
          }
        },
        "overrides": []
      },
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "rawQueryText": "with first as (\n    select h.session, min(h.hit_id) as hit_id\n    from hits h join paths p using (path_id)\n    where p.event = 0 and h.created_at >= datetime('now', '-' || ${days} || ' days')\n    group by h.session)\nselect coalesce(nullif(replace(replace(replace(p.path, 'playground.ghul.dev/run/', 'playground-run/'), '/playground.ghul.dev', '/playground'), '/ghul.dev', ''), ''), '/') as entry_page, count(*) as visits\nfrom first join hits h using (hit_id) join paths p using (path_id)\ngroup by entry_page order by visits desc limit 25",
          "queryText": "with first as (\n    select h.session, min(h.hit_id) as hit_id\n    from hits h join paths p using (path_id)\n    where p.event = 0 and h.created_at >= datetime('now', '-' || ${days} || ' days')\n    group by h.session)\nselect coalesce(nullif(replace(replace(replace(p.path, 'playground.ghul.dev/run/', 'playground-run/'), '/playground.ghul.dev', '/playground'), '/ghul.dev', ''), ''), '/') as entry_page, count(*) as visits\nfrom first join hits h using (hit_id) join paths p using (path_id)\ngroup by entry_page order by visits desc limit 25",
          "timeColumns": [],
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": "analytics"
          }
        }
      ]
    },
    {
      "type": "table",
      "title": "Where they stopped",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "analytics"
      },
      "gridPos": {
        "h": 10,
        "w": 12,
        "x": 12,
        "y": 14
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm",
        "footer": {
          "show": false,
          "reducer": [
            "sum"
          ],
          "fields": ""
        }
      },
      "fieldConfig": {
        "defaults": {
          "custom": {
            "align": "auto",
            "filterable": true
          }
        },
        "overrides": []
      },
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "rawQueryText": "with last as (\n    select h.session, max(h.hit_id) as hit_id, count(*) as n\n    from hits h join paths p using (path_id)\n    where p.event = 0 and h.created_at >= datetime('now', '-' || ${days} || ' days')\n    group by h.session)\nselect coalesce(nullif(replace(replace(replace(p.path, 'playground.ghul.dev/run/', 'playground-run/'), '/playground.ghul.dev', '/playground'), '/ghul.dev', ''), ''), '/') as exit_page, count(*) as visits, sum(last.n = 1) as of_which_bounced\nfrom last join hits h using (hit_id) join paths p using (path_id)\ngroup by exit_page order by visits desc limit 25",
          "queryText": "with last as (\n    select h.session, max(h.hit_id) as hit_id, count(*) as n\n    from hits h join paths p using (path_id)\n    where p.event = 0 and h.created_at >= datetime('now', '-' || ${days} || ' days')\n    group by h.session)\nselect coalesce(nullif(replace(replace(replace(p.path, 'playground.ghul.dev/run/', 'playground-run/'), '/playground.ghul.dev', '/playground'), '/ghul.dev', ''), ''), '/') as exit_page, count(*) as visits, sum(last.n = 1) as of_which_bounced\nfrom last join hits h using (hit_id) join paths p using (path_id)\ngroup by exit_page order by visits desc limit 25",
          "timeColumns": [],
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": "analytics"
          }
        }
      ]
    },
    {
      "type": "table",
      "title": "Commonest steps from one page to the next",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "analytics"
      },
      "gridPos": {
        "h": 10,
        "w": 12,
        "x": 0,
        "y": 24
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm",
        "footer": {
          "show": false,
          "reducer": [
            "sum"
          ],
          "fields": ""
        }
      },
      "fieldConfig": {
        "defaults": {
          "custom": {
            "align": "auto",
            "filterable": true
          }
        },
        "overrides": []
      },
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "rawQueryText": "with seen as (\n    select h.session, h.hit_id, coalesce(nullif(replace(replace(replace(p.path, 'playground.ghul.dev/run/', 'playground-run/'), '/playground.ghul.dev', '/playground'), '/ghul.dev', ''), ''), '/') as path,\n           lead(coalesce(nullif(replace(replace(replace(p.path, 'playground.ghul.dev/run/', 'playground-run/'), '/playground.ghul.dev', '/playground'), '/ghul.dev', ''), ''), '/')) over (partition by h.session order by h.hit_id) as next_path\n    from hits h join paths p using (path_id)\n    where p.event = 0 and h.created_at >= datetime('now', '-' || ${days} || ' days'))\nselect path as from_page, next_path as to_page, count(*) as times\nfrom seen where next_path is not null and next_path <> path\ngroup by path, next_path order by times desc limit 25",
          "queryText": "with seen as (\n    select h.session, h.hit_id, coalesce(nullif(replace(replace(replace(p.path, 'playground.ghul.dev/run/', 'playground-run/'), '/playground.ghul.dev', '/playground'), '/ghul.dev', ''), ''), '/') as path,\n           lead(coalesce(nullif(replace(replace(replace(p.path, 'playground.ghul.dev/run/', 'playground-run/'), '/playground.ghul.dev', '/playground'), '/ghul.dev', ''), ''), '/')) over (partition by h.session order by h.hit_id) as next_path\n    from hits h join paths p using (path_id)\n    where p.event = 0 and h.created_at >= datetime('now', '-' || ${days} || ' days'))\nselect path as from_page, next_path as to_page, count(*) as times\nfrom seen where next_path is not null and next_path <> path\ngroup by path, next_path order by times desc limit 25",
          "timeColumns": [],
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": "analytics"
          }
        }
      ]
    },
    {
      "type": "table",
      "title": "How they arrived",
      "datasource": {
        "type": "frser-sqlite-datasource",
        "uid": "analytics"
      },
      "gridPos": {
        "h": 10,
        "w": 12,
        "x": 12,
        "y": 24
      },
      "options": {
        "showHeader": true,
        "cellHeight": "sm",
        "footer": {
          "show": false,
          "reducer": [
            "sum"
          ],
          "fields": ""
        }
      },
      "fieldConfig": {
        "defaults": {
          "custom": {
            "align": "auto",
            "filterable": true
          }
        },
        "overrides": []
      },
      "targets": [
        {
          "refId": "A",
          "queryType": "table",
          "rawQueryText": "select\n    case when r.ref = '' then '(typed or bookmarked)' else r.ref end as referrer,\n    count(distinct h.session) as visits\nfrom hits h left join refs r using (ref_id)\nwhere h.created_at >= datetime('now', '-' || ${days} || ' days') and h.first_visit = 1\ngroup by referrer order by visits desc limit 25",
          "queryText": "select\n    case when r.ref = '' then '(typed or bookmarked)' else r.ref end as referrer,\n    count(distinct h.session) as visits\nfrom hits h left join refs r using (ref_id)\nwhere h.created_at >= datetime('now', '-' || ${days} || ' days') and h.first_visit = 1\ngroup by referrer order by visits desc limit 25",
          "timeColumns": [],
          "datasource": {
            "type": "frser-sqlite-datasource",
            "uid": "analytics"
          }
        }
      ]
    }
  ]
}
